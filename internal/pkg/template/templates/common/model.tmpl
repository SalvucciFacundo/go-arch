package {{ if eq .Architecture "Hexagonal" }}domain{{ else }}model{{ end }}

import (
	"time"
)

// {{ .EntityName }} represents the core business model.
type {{ .EntityName }} struct {
	ID        uint      `json:"id"`
	Name      string    `json:"name"`
	CreatedAt time.Time `json:"created_at"`
	UpdatedAt time.Time `json:"updated_at"`
}
