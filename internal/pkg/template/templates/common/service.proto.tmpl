syntax = "proto3";

package {{ .ProjectName | lower }};

option go_package = "{{ .ModuleName }}/internal/adapters/grpc/proto";

// {{ .ProjectName | title }}Service defines the main service contract.
service {{ .ProjectName | title }}Service {
  // HealthCheck checks the service health.
  rpc HealthCheck(HealthRequest) returns (HealthResponse);
}

message HealthRequest {
  string service_name = 1;
}

message HealthResponse {
  string status = 1;
  string message = 2;
}
